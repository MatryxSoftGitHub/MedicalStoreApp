<%- include('header') -%>
<div class="login_main" id="login">
    <div class="title">
        <h2 class="text-weight-bold">Medical Store System</h2>
        <h6>Login for the registered user</h6>
    </div>
    <div class="login-container">
        <form action="/route/api/medical/auth/login" method="POST" class="pt-3">
            <div class="form-group">
                <input type="text" class="form-control" id="user_name" name="user_name" placeholder="Username" autocomplete="off" pattern="[a-zA-Z]{0,20}" required>
                <!-- <small class="form-text text-muted text-left">Register email address</small> -->
                <!-- <input type="password" class="form-control" name="password" placeholder="Password"> -->
            </div>
            <div class="form-group">
                <input type="password" class="form-control" id="password" name="password" placeholder="Password" autocomplete="off" pattern=".{0,20}" required>
            </div>
            <button type="submit" class="btn btn-success rounded-pill">Login</button>
        </form>
        <% if(locals.logout){ %>
            <div class="alert alert-success text-center">
             <%= logout %>
            </div>
         <% } %>    
    </div>
</div>
<%- include('footer') -%>